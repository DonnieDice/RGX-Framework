# RGX-Framework 2.7.0

Released 2026-08-15.

## Declarative Named Repeating Timers

- Implements `every = { name = { seconds, handler } }` as a complete declarative
  vertical slice. Declarative `on` remains Tier 4.
- Validates printable names containing a non-space character, exact two-item
  tuples, finite positive intervals, and function handlers before registering
  the addon, slash commands, events, or timers.
- Binds timers after the matching consumer `ADDON_LOADED`, once its database,
  options panel, and minimap button are ready. The first call occurs after one
  complete interval.
- Calls handlers as `handler(addon, timer)` and supports self-cancellation with
  `addon:CancelTimer(timer)`.
- Adds owner/name metadata and stable `AddonName:every:name` labels. Timers in
  one declaration that become due together dispatch in lexical name order.
- Keeps failures isolated: one timer error is labeled and reported without
  preventing unrelated due timers from running.
- Rejects duplicate addon declarations before any resources can be replaced or
  leaked.

## Contract And Verification

- Promoted `every` to `x-rgx-ships: today` in the canonical schema while keeping
  `on` at `tier4`.
- Added 14 schema behavior vectors, strict non-finite-number rejection, MCP
  validation/generation, deterministic and safely quoted generated timer names,
  and RGX-Hello E2E coverage.
- Added a real Lua 5.1 runtime harness with 166 checks for validation, lifecycle,
  ordering, metadata, self-cancellation, duplicate rejection, and failure
  isolation.

## Accessible-Only Aura Boundary

- Uses `canaccessvalue`/`canaccesstable` as the primary permission predicates,
  with secrecy classifiers and verified no-secret flavor behavior as fallbacks.
- Checks event-wide, index, instance, table, and scalar access before aura data
  is queried, indexed, iterated, compared, cached, formatted, or forwarded.
- Returns no result and suppresses callbacks for restricted or unverifiable
  data. Watched caches invalidate silently and rebuild on the next accessible
  event without replaying missed transitions.
- Moves internal Combat and Delves aura listeners onto guarded unit-event
  routing while documenting raw `RegisterEvent("UNIT_AURA")` as unsanitized.
- Adds 86 Lua 5.1 restricted-boundary checks plus deterministic safe/unsafe
  source-audit fixtures. The audit catches protected/stored raw API references,
  fails closed on parse errors, and pairs with RGX-Hello phase counter snapshots.

## Release Assets

- `RGX-Framework-v2.7.0.zip` - the only product archive; 100 runtime files under
  one `RGX-Framework/` root with all six flavor TOCs.
- `release.json` - automation metadata for Retail `120100`, Era `11509`, TBC
  `20506`, Wrath/Titan `38002`, Cataclysm `40402`, and Mists `50504`.
